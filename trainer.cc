#include "Dictionary.h"

using namespace sm;

int
main(){
  while (){
    //get data
    string content, title;
    Dictionary dict;
    
    Document doc(content, title);
    dict.add (doc);

    dict.save ("xxx")

    
  }
}
